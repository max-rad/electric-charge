mixin section(data)
  section.section
    .container
      .section__wrapper
        h2.title.title--h2.section__title!=data.title
        if (data.description)
          .section__content
            each item in data.description
              p!=item
        if (block)
          block
